from typing import Optional

#def to_power(x: Optional[int, float], exp: int):

#    if exp == 1:
#        return exp
#    elif x <= 0 or exp <= 0:
#        raise ValueError("This function works only with exp > 0.")
